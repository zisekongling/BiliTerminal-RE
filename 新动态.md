基于你提供的两个API示例（空间预约API和空间动态Feed API），我为你整理了一份详细的API使用文档。文档主要聚焦于**动态Feed API**（`/x/polymer/web-dynamic/v1/feed/space`），因为它结构最复杂、使用最广泛。

---

# B站用户空间动态Feed API 文档

## 1. 概述

该API用于获取B站用户主页“动态”标签页中的内容列表。它返回结构化的模块化数据，支持视频、图文、转发、投票等多种动态类型，并包含分页游标机制。

- **接口地址**：`https://api.bilibili.com/x/polymer/web-dynamic/v1/feed/space`
- **请求方式**：`GET`
- **认证方式**：WBI签名（`w_rid` + `wts`）+ 浏览器指纹（可选但强烈建议）

## 2. 请求参数

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| `host_mid` | int | 是 | 目标用户的UID | `591904067` |
| `offset` | string | 否 | 分页游标，首次请求可不传或传空字符串；后续请求使用上次返回的`offset` | `"1005403706004340742"` |
| `timezone_offset` | int | 是 | 客户端时区偏移（分钟），东八区为`-480` | `-480` |
| `platform` | string | 是 | 平台标识，固定`web` | `web` |
| `features` | string | 是 | 特性开关列表，逗号分隔，用于控制返回字段和样式 | `itemOpusStyle,listOnlyfans,...` |
| `web_location` | string | 否 | 来源标识，通常为`0.0` | `0.0` |
| `w_rid` | string | 是 | WBI签名，32位MD5小写 | `affb979abf9941f8f2db471a487455a9` |
| `wts` | int | 是 | 请求发起时的Unix时间戳（秒） | `1780498616` |
| `dm_img_list` | string | 否 | 浏览器指纹（鼠标轨迹等）JSON数组的URL编码 | `[%7B%22x%22...]` |
| `dm_img_str` | string | 否 | 浏览器UA/Canvas指纹哈希 | `V2ViR0wgMS4w...` |
| `dm_cover_img_str` | string | 否 | WebGL/GPU指纹 | `QU5HTEUgKEludGVs...` |
| `dm_img_inter` | string | 否 | 交互行为指纹JSON | `%7B%22ds%22...` |
| `x-bili-device-req-json` | string | 否 | 设备信息（平台、设备类型等） | `%7B%22platform%22...` |

> **WBI签名算法简述**：  
> 1. 将所有请求参数（除`w_rid`、`wts`外）按键名升序排序。  
> 2. 将排序后的参数拼接为 `key1=value1&key2=value2...`，再拼接上`wts`的值和固定密钥（动态获取）。  
> 3. 对上述字符串计算MD5，得到`w_rid`。  
> 4. 实际使用时，`wts`和`w_rid`同时附加在URL上。  
> （具体密钥需从B站首页的`nav`接口获取，此处不展开）

## 3. 响应结构

### 3.1 顶层响应

```json
{
    "code": 0,
    "message": "0",
    "data": { ... },
    "ttl": 1
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `code` | int | 0表示成功；非0为错误码（如-400参数错误，-412签名验证失败） |
| `message` | string | 状态信息，成功时通常为`"0"` |
| `ttl` | int | 建议缓存时间（秒） |
| `data` | object | 核心业务数据 |

### 3.2 `data` 对象

```json
{
    "update_num": "0",
    "update_baseline": "",
    "offset": "1005403706004340742",
    "has_more": true,
    "total": "0",
    "items": [ ... ]
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `update_num` | string | 新动态数量（用于增量更新，通常不准确） |
| `update_baseline` | string | 增量更新的基准时间 |
| `offset` | string | **下一页游标**，请存储并用于下一次请求 |
| `has_more` | bool | `true` 表示还有更多数据，`false` 表示已到末尾 |
| `total` | string | 动态总数（他人空间不准确，通常为`"0"`） |
| `items` | array | 动态条目列表 |

### 3.3 `items` 数组中的动态对象

每个动态对象包含以下核心字段：

```json
{
    "id_str": "1040901932844056595",
    "type": "DYNAMIC_TYPE_FORWARD",
    "basic": { ... },
    "visible": true,
    "modules": { ... },
    "orig": null  // 当 type 为转发时，orig 存在
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `id_str` | string | 动态唯一ID |
| `type` | string | 动态类型，见下表 |
| `basic` | object | 基础元信息（评论类型、是否审核中等） |
| `visible` | bool | 是否可见（被删除或仅粉丝可见会为false） |
| `modules` | object | **核心内容模块**（作者、正文、统计等） |
| `orig` | object | 当`type`为转发时，此处存放原始动态（结构与当前动态一致） |

#### 常见动态类型 `type`

| 类型常量 | 说明 |
|----------|------|
| `DYNAMIC_TYPE_AV` | 投稿视频动态 |
| `DYNAMIC_TYPE_FORWARD` | 转发动态（原内容在`orig`中） |
| `DYNAMIC_TYPE_DRAW` | 图文动态（opus） |
| `DYNAMIC_TYPE_ARTICLE` | 专栏动态 |
| `DYNAMIC_TYPE_LIVE` | 直播动态 |
| `DYNAMIC_TYPE_WORD` | 纯文本动态 |

### 3.4 `modules` 模块详解

#### 3.4.1 `module_author` - 作者信息

```json
"module_author": {
    "name": "我的世界紫色",
    "face": "https://i2.hdslb.com/bfs/face/...",
    "mid": 591904067,
    "pub_ts": "1741192267",
    "pub_time": "2025年03月06日",
    "following": 2,
    "vip": { "type": 1, "label": { "text": "" } },
    "official_verify": { "type": -1, "desc": "" },
    "decoration_card": { ... }   // 装扮卡片
}
```

| 字段 | 说明 |
|------|------|
| `name` | 用户名 |
| `face` | 头像URL |
| `mid` | 用户UID |
| `pub_ts` | 发布时间戳（秒） |
| `pub_time` | 格式化后的时间字符串 |
| `following` | 当前登录用户是否关注：`1`=未关注，`2`=已关注 |
| `vip` | 大会员信息（`type`: 0=无,1=大会员,2=年度大会员） |
| `decoration_card` | 粉丝装扮卡片（含名字、编号、颜色等） |

#### 3.4.2 `module_dynamic` - 动态主体

这是最复杂的模块，包含动态的文本、富文本、主要载体（视频/图文等）。

```json
"module_dynamic": {
    "desc": {
        "text": "我参与了投票​您打算抽遐蝶玛？ 我投给了“抽2+1”",
        "rich_text_nodes": [ ... ]
    },
    "major": {
        "type": "MAJOR_TYPE_ARCHIVE",
        "archive": { ... }   // 视频信息
    },
    "additional": { ... }    // 附加组件（投票、游戏卡片等）
}
```

- **`desc`**：动态正文，支持富文本节点（`rich_text_nodes`）。节点类型包括：
  - `RICH_TEXT_NODE_TYPE_TEXT`：普通文本
  - `RICH_TEXT_NODE_TYPE_EMOJI`：表情
  - `RICH_TEXT_NODE_TYPE_TOPIC`：话题（`#xxx#`）
  - `RICH_TEXT_NODE_TYPE_AT`：@用户
  - `RICH_TEXT_NODE_TYPE_BV`：视频链接（自动展开）
  - `RICH_TEXT_NODE_TYPE_LOTTERY`：抽奖组件
  - `RICH_TEXT_NODE_TYPE_VOTE`：投票组件

- **`major`**：主要载体，决定了动态的核心展示内容。其`type`字段常见值：
  - `MAJOR_TYPE_ARCHIVE`：视频（`archive`对象含`bvid`, `title`, `cover`, `stat`）
  - `MAJOR_TYPE_OPUS`：图文（`opus`对象含`summary`, `pics`数组）
  - `MAJOR_TYPE_DRAW`：旧版图文（已较少使用）
  - `MAJOR_TYPE_LIVE`：直播（含`live`对象）

- **`additional`**：附加组件，例如：
  - `ADDITIONAL_TYPE_VOTE`：投票
  - `ADDITIONAL_TYPE_COMMON`：游戏推荐卡片（`common.sub_type="game"`）

#### 3.4.3 `module_stat` - 互动统计

```json
"module_stat": {
    "forward": { "count": 0, "status": false },
    "comment": { "count": 0, "status": false },
    "like": { "count": 0, "status": false }
}
```

| 字段 | 说明 |
|------|------|
| `count` | 总数 |
| `status` | 当前登录用户是否已操作（点赞/转发/评论） |

#### 3.4.4 `module_more` - 更多操作

```json
"module_more": {
    "three_point_items": [
        { "label": "置顶", "type": "THREE_POINT_TOP", "params": {...} },
        { "label": "删除", "type": "THREE_POINT_DELETE", "modal": {...} }
    ]
}
```

仅在**当前登录用户自己的动态**中出现，用于展示“...”菜单中的操作项（置顶、删除、举报等）。

### 3.5 `orig` - 转发源内容

当`type == "DYNAMIC_TYPE_FORWARD"`时，`orig`字段存在，其结构与父级动态完全相同。可以递归解析。

## 4. 分页使用示例

### 第一次请求
```http
GET /x/polymer/web-dynamic/v1/feed/space?host_mid=591904067&offset=&timezone_offset=-480&platform=web&features=...&w_rid=xxx&wts=xxx
```
响应中取 `data.offset` 和 `data.has_more`。

### 第二次请求（加载更多）
```http
GET /x/polymer/web-dynamic/v1/feed/space?host_mid=591904067&offset=1005403706004340742&timezone_offset=-480&platform=web&features=...&w_rid=yyy&wts=yyy
```
直到 `has_more == false`。

## 5. 注意事项

1. **WBI签名是必须的**  
   缺少`w_rid`或签名错误会返回`-412`错误。签名密钥需从B站首页`nav`接口动态获取（`wbi_img_url`等字段）。  

2. **浏览器指纹参数非必须但强烈建议**  
   缺少指纹参数可能导致风控升级（如验证码或请求频率限制）。最简单的做法是从真实浏览器复制完整的`dm_img_*`参数。  

3. **`features` 参数会影响返回字段**  
   建议复制浏览器中实际使用的`features`值，否则可能缺失某些模块（如`opus`内容）。  

4. **`offset` 是字符串类型**  
   不要转换为整数，直接作为字符串使用。  

5. **动态数据可能为空**  
   当用户没有动态或设置了隐私权限时，`items`为空数组，`has_more`为false。  

6. **注意时间戳单位**  
   `pub_ts` 是秒级Unix时间戳，`wts` 也是秒级。  

## 6. 快速调试建议

- 直接在浏览器开发者工具的Network面板中复制完整的动态请求URL，该URL已包含正确的签名和指纹。
- 使用Python的`requests`库时，务必设置相同的`User-Agent`和`Referer`（通常为`https://space.bilibili.com/`）。
- 若遇到`-412`错误，请检查`w_rid`是否与当前`wts`匹配，或`wts`是否与服务器时间相差超过5分钟。

---

以上文档基于你提供的实际API请求和响应示例整理而成。如需更详细的字段说明（如`opus`内部结构或`decoration_card`的具体含义），可进一步提供更多示例数据。